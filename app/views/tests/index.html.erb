<h1><p class="text-center"><%= t('tests.index.header') %></p></h1>

<table class="table">
  <thead>
    <tr>  
      <th scope="col"><%= t('tests.index.title') %></th>
      <th scope="col"><%= t('tests.index.level') %></th>
      <th scope="col"><%= t('tests.index.questions') %></th>
      <th scope="col"><%= t('tests.index.actions') %></th>
    </tr>
  </thead>

  <%= render @tests %>

</table>  
