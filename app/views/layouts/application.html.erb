<!DOCTYPE html>
<html>
  <head>
    <title>TestGuru</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", media: 'all', "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <% if logged_in? %>
      <p class='nav user_tg'><b><%= link_to 'Test Guru', tests_path %></b></p>
      <p class='nav user_welcome'>Welcome, <b><%= current_user.name %> Guru</b></p>
      <p class='nav user_out'><%= link_to 'Log out', logout_path, data: {turbo_method: :delete} %></p>
    <% else %>
      <p class='nav non_user'><b><%= link_to 'Sign up', signup_path %></b></p>
      <p class='nav non_user'><b><%= link_to 'Log in', login_path %></b></p>
    <% end %>

    <%= yield %><br>

    <br><%= render 'layouts/footer' %>
  </body>
</html>
